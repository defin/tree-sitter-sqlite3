# Allowlist for the differential harness (scripts/differential-test.py).
#
# Lists SS-AR cases (sqlite accepts, we reject) that are permitted to
# fail. Hard rule: this file should shrink over time as grammar gaps
# get fixed.
#
# Format: <path>:<line>  # rationale

# These three fragments demonstrate sqlite's lenient parse-level
# acceptance of syntactically suspect inputs that fail at the
# semantic / runtime level. Our grammar rejects them at parse time,
# which is arguably better behavior. Deliberately allowlisted so the
# differential harness doesn't fail CI on them.
vendor/sqlite-test-corpus/test/alter.test:323     # `ALTER TABLE t3 ADD COLUMN (...)` — sqlite tokenizer accepts the trailing `(`; our grammar requires a name+typename per parse.y `columnname`.
vendor/sqlite-test-corpus/test/trigger1.test:240  # trigger body containing invalid `SELECT * FROM;` — sqlite stores the body text as-is and only compiles it on trigger fire, so prepare doesn't see the syntax error.
vendor/sqlite-test-corpus/test/window1.test:1921  # fuzz-generated heavily-nested window-function CASE expression with `-true` and `CAST(a AS )` empty-type forms; sqlite is exceptionally lenient here.
vendor/sqlite-test-corpus/test/trigger1.test:834  # trigger body contains `LIMIT 1,#1` — `#N` bind-parameter form is rejected by sqlite at top level (verified) but trigger bodies are stored verbatim and only fully parsed at fire-time.
